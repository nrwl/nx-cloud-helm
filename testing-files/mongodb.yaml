global:
  security:
    allowInsecureImages: true

architecture: replicaset
replicaCount: 1

image:
  repository: bitnamilegacy/mongodb

auth:
  enabled: false

arbiter:
  enabled: false

persistence:
  enabled: false

metrics:
  enabled: false